/*#include<cstdio>
int main()
{
	int f;
	int s[5];
	int count=0;
	scanf("%d",&f);
	for(int i=0;i<5;i++)
	{
		scanf("%d",&s[i]);
		if(f==s[i])
		{
			count++;
		}
		
	}

	printf("%d대",count);
}*/
#include<cstdio>
int main()
{
	int a[4], m[4];
	int max=0;
	int curr = 0;
	for(int i=0;i<4;i++)
	{
		scanf("%d %d",&a[i],&m[i]);
		
	}
	for (int i = 0; i < 4; i++)
	{
		curr = curr - a[i] + m[i];
		if (curr > max)
		{
			max = curr;
		}
	}
    printf("%d",max);
	
	
}
