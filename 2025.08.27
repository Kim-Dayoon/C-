/*#include<cstdio>
#include<algorithm>
using namespace std;
bool cmp(int left,int right)
{
	return left>right;
}
int main()
{
	int a[10];
	for(int i=0;i<10;i++)
	{
		scanf("%d",&a[i]);
	}
	sort(a,a+10);
	for(int i=0;i<10;i++)
	{
		printf("%d ",a[i]);
	}
	printf("\n");
	sort(a,a+10,cmp);
	for(int i=0;i<10;i++)
	{
		printf("%d ",a[i]);
	}
}*/

/*#include<cstdio>
#include<algorithm>
using namespace std;
int main()
{
	float a[1000];
	float b[1000];
	int n;
	int count=0;
	scanf("%d",&n);
	for(int i=0;i<n;i++)
	{
		scanf("%f",&a[i]);
		b[i]=a[i];
	}
	sort(b,b+n);
	for(int i=0;i<n;i++)
	{
		if(a[i]!=b[i])
		{
			count++;
		}
	}
	printf("%d",count);
	
}*/

/*#include<cstdio>
#include<algorithm>
using namespace std;
int main()
{
	int n;
	pair<int,int>P[1000];
	scanf("%d",&n);
	for(int i=0;i<n;i++)
	{
		scanf("%d%d",&P[i].first,&P[i].second);
	}
	sort(P,P+n);
	for(int i=0;i<n;i++)
	{
		if(i>=1&&P[i].first!=P[i-1].first)
		{
			printf("\n");
		}
		printf("%d ",P[i].second);
	}

}*/

#include<cstdio>
#include<algorithm>
using namespace std;
float cal_e(pair<int,int>a)
{
	return 0.5*a.first*a.second*a.second;
}
bool cmp(pair<int,int>left,<int,int>right)
int main()
{
	int n;
	pair
	
}
